# Database (PostgreSQL). Dev/tests: use connection URI from Supabase Dashboard (Project Settings → Database). Production: Supabase or other Postgres.
# Exact URI is in Dashboard → Database (Session and/or Transaction mode). Example (pooler):
# When using Supabase pooler (transaction mode), append ?pgbouncer=true to DATABASE_URL so Prisma does not use prepared statements (avoids "prepared statement already exists" in serverless).
# DATABASE_URL="postgresql://postgres.[PROJECT-REF]:[YOUR-PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres?pgbouncer=true"
# DIRECT_URL = Session mode URI for migrations (same as DATABASE_URL if not using pooler).
DATABASE_URL="postgresql://postgres.[PROJECT-REF]:[YOUR-PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres?pgbouncer=true"
DIRECT_URL="postgresql://postgres.[PROJECT-REF]:[YOUR-PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres"

# Teacher login (Google OAuth)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET=

# Scenario explanations (Gemini). Default model: gemini-2.0-flash. Override with GEMINI_MODEL if needed (e.g. gemini-1.5-flash-001).
GEMINI_API_KEY=
# GEMINI_MODEL=

# Magic link (trainee return). Production: set all three so links work.
# RESEND_API_KEY: required in production; get from Resend dashboard.
# MAGIC_LINK_FROM_EMAIL: verified sender (e.g. noreply@coefficient.work) or leave unset to use onboarding@resend.dev.
# NEXT_PUBLIC_APP_URL: base URL for magic links (e.g. https://coefficient.work); without it, links may point to wrong origin.
RESEND_API_KEY=
MAGIC_LINK_FROM_EMAIL=

# Production: set so magic links use correct origin (e.g. https://coefficient.work).
NEXT_PUBLIC_APP_URL=

# Field Video Assessment — Supabase Storage (same project as DATABASE_URL). From Supabase Dashboard: Project Settings → API.
NEXT_PUBLIC_SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
